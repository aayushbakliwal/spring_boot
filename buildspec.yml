version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - java -version
      - ./mvnw clean package

  post_build:
    commands:
      - echo nothing in post build.

artifacts:
  files: target/spring_boot-0.0.1-SNAPSHOT.war
